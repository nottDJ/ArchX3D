/**
 * ArchX3D — generation icons (compatibility shim)
 * ===============================================
 * Re-exports from `@/components/ui/icons`. See `wizard/icons.tsx` for why.
 */

export {
  ArrowDownIcon,
  CheckIcon,
  CopyIcon,
  CubeIcon,
  RetryIcon,
  SpinnerIcon,
  CloseIcon as CrossIcon,
} from "@/components/ui/icons";
